const Button = ({ text }) => {
  return (
    <button className="font-semibold tracking-wider px-8 py-3 text-white rounded-md text-lg bg-gray-800">
      {text}
    </button>
  );
};

export default Button;
